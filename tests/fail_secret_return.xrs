// EXPECT: FAIL [R5 secret-leak-via-return] — unscoped fn returns secret-derived data.
model User {
  id: String
  secret password_hash: String
}

fn sneak(user: User) -> String {
  return user.password_hash
}
