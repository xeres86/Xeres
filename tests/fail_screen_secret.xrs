// EXPECT: FAIL [R3 secret-containment] — a screen (runs in the browser) reads a secret field.
model User {
  id: String
  secret password_hash: String
}

ui screen Profile(user: User) {
  view {
    text user.password_hash
  }
}
