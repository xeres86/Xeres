// private_card.xrs — `ui component` WITHOUT `pub`, so it is module-private.
// Imported by fail_component_not_pub.xrs; the cross-module reference is R35.
ui component Badge(label: String) {
    view {
        box { text label }
    }
}
