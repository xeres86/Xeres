// the tier boundary holds THROUGH a component: reading a `secret` field inside
// a `ui component` is rejected exactly as in any other browser code (R3)
model User { id: String  username: String  secret token: String }

ui component UserCard(u: User) {
  view { text u.token }
}

ui screen S {
  view { column { text "ok" } }
}
