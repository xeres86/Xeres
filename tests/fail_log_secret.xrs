// R27 log-no-secret — passing a secret value to `log` is a compile error, so a
// credential can never leak through logs.
model User {
  id: String
  username: String
  secret password_hash: String
}

server fn leak(u: User) -> String {
  log.info(u.password_hash)
  return u.id
}
