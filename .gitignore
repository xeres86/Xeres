# Rust build output
/target
**/target

# Generated app output (regenerated from .xrs on every build)
/out
**/out

# Node / scaffolded deps and bundles
node_modules/
package-lock.json
**/client.js

# Local tooling / scratch
.claude/
_*/
*.log

# Perf harness (spec 30): timestamped run outputs are local; only the checked-in
# bench/baseline.json is tracked (the regression gate).
bench/results-*.json

# Secrets / local config — never commit connection strings
.env
.env.*
!.env.example
